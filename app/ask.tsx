import { AskPockeetScreen } from "@/features/ask/components/AskPockeetScreen";

/**
 * Ask Pockeet — canned answers from local receipt data (MVP+).
 * @see docs/ux/screens/ask-pockeet.md
 */
export default function AskScreen() {
  return <AskPockeetScreen />;
}
