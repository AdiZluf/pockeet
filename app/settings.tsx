import { SettingsScreen } from "@/features/settings/components/SettingsScreen";

/**
 * Settings modal — language and preferences.
 * @see docs/ux/screens/settings.md
 */
export default function SettingsRoute() {
  return <SettingsScreen />;
}
